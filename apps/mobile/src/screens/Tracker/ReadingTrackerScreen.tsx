import React from 'react';
import { Text, View } from 'react-native';

export function ReadingTrackerScreen(): JSX.Element {
  return (
    <View>
      <Text>30-day Plan + Custom Plan + Progress + Streak</Text>
    </View>
  );
}
